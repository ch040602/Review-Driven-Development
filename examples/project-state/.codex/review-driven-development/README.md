# Example project state for review-driven-development

This directory shows where a target project stores persistent RDD state.

Copy this layout into a project only as a reference. Do not treat these sample defaults as final user preferences.
